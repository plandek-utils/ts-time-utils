{
  "name": "@plandek-utils/time-utils",
  "version": "2.1.1",
  "author": "Eduardo Turiño <eturino@plandek.com>",
  "description": "Small utils for managing Time in tests, using timekeeper for time manipulation.",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "commit": "git-cz",
    "build": "tsup",
    "dev": "tsup --watch",
    "check": "biome check --write src",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "reset": "git clean -dfx -e .idea && git reset --hard && npm install",
    "prepare-release": "npm run reset && npm run check && npm run test && light-release && npm run build",
    "prepare": "husky || true"
  },
  "repository": {
    "type": "github",
    "url": "git+https://github.com/plandek-utils/time-utils.git"
  },
  "bugs": {
    "url": "https://github.com/plandek-utils/time-utils/issues"
  },
  "homepage": "https://github.com/plandek-utils/time-utils",
  "license": "MIT",
  "keywords": [],
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "@commitlint/cz-commitlint": "^19.5.0",
    "@types/node": "^22.9.0",
    "@vitest/coverage-v8": "^2.1.4",
    "commitizen": "^4.3.1",
    "husky": "^9.1.6",
    "inquirer": "^12.3.3",
    "light-release": "^2.4.2",
    "timekeeper": "^2.3.1",
    "ts-enum-util": "^4.1.0",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  },
  "peerDependencies": {
    "timekeeper": "^2.3.1"
  },
  "config": {
    "commitizen": {
      "path": "@commitlint/cz-commitlint"
    }
  }
}